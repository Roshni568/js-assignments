// Create an array
const myArray = ["apple", "banana", "orange", "grape"];

// Create a new array from index 1 to 3 (exclusive)
const newArray = myArray.slice(1, 3);

// Print the new array
console.log(newArray);
